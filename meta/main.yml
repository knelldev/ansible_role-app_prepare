galaxy_info:
  author: knelldev
  description: This include_role creates the user, the group and the directories for an app.
  company: knelldev

  role_name: app_prepare

  license: license (MIT)

  min_ansible_version: 2.9
  platforms:
  - name: Debian
    versions:
      - stretch
      - buster
  - name: Ubuntu
    versions:
      - xenial
      - bionic
      - focal

  galaxy_tags: 
    - linux
    - system
    - development
    - app
    - application
    - prepare

dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.